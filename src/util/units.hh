/**
 * @file units.hh
 * @brief Unit-specific aliases for clarity.
 * 1 spritepx (size of 1 pixel from a sprite) = 2 px
 * 1 tile (size of a tile in spritepx) = usually 16 spritepx
 */

#pragma once

namespace yumeami {

  using tile_int = int;
  using tile_float = float;
  using spritepx_int = int;

} // namespace yumeami
