#pragma once
namespace yumeami {

  struct TrueTilePos;
  struct DrawTilePos;
  struct TexelPos;
  struct Velocity;
  struct Facing;

  struct PlayerTag;
  struct CollisionTag;
  struct CameraTargetTag;

} // namespace yumeami
