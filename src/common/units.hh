/**
 * @file units.hh
 * @brief Unit-specific aliases for clarity.
 * 1 spritepx/spx (size of 1 pixel from a sprite) = 2 px
 * 1 tile (size of a tile in spritepx) = usually 16 spritepx
 * NOTE: values in px will use bare types.
 */

#pragma once

namespace yumeami {

  using tile_int = int;
  using tile_float = float;
  using spx_int = int;

} // namespace yumeami
